[pytest]
addopts = -ra
markers =
    api: API tests against the demo app
    ui: Browser tests against the demo app
    data: Data quality checks
    llm: LLM quality evaluation tests
    expected_failure: Known issue intentionally documented by the portfolio
    requires_openai: Test calls the OpenAI API and needs OPENAI_API_KEY
